program prueba ;{ Archivo Prueba C&I Semántico}
var
  a, b: integer;
  l:boolean;

function algo(m,n:boolean): integer;
 var b:boolean;
begin
  algo := a + 5;
end;

procedure nuevo (a,b,c: integer);
begin
  write ( a );
end;

begin
   a:=9 + algo(l,l and TRUE,TRUE);
   nuevo(a*2);
end.

